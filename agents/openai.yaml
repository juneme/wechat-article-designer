interface:
  display_name: "WeChat Article Designer"
  short_description: "Create, validate, and deliver WeChat articles"
  default_prompt: "Use $wechat-article-designer to complete every writing and design gate, then use the enforced release command: create a new draft automatically when all three console variables are present and the backend is healthy, otherwise deliver only a local preview."
