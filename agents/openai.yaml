interface:
  display_name: "WeChat Article Designer"
  short_description: "Write and freely art-direct WeChat articles"
  default_prompt: "Use $wechat-article-designer to write and freely compose an original WeChat article, then run postflight safety and publishing checks. When all three console variables are present and the backend is healthy, create a new draft automatically; otherwise deliver local preview."
